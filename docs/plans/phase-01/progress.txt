## Cycle 1 — Scaffold the frontend app with the approved toolchain
Date: 2026-03-18

Summary:
- Created the Vite React TypeScript application from scratch.
- Added package.json with all ADR-specified dependencies: React 19, TypeScript,
  Vite, TanStack Query, React Router, Tailwind CSS, Vitest, React Testing
  Library, Playwright, ESLint (typescript-eslint, react-hooks, react-refresh,
  security plugins), and Prettier.
- Added baseline scripts: dev, build, test, e2e.
- Created minimal app root (src/main.tsx, src/app/App.tsx) with QueryClientProvider
  and BrowserRouter shell.
- Added src/test-setup.ts importing @testing-library/jest-dom.
- Added Tailwind CSS config (tailwind.config.js, postcss.config.js), ESLint flat
  config (eslint.config.js), Prettier config (.prettierrc), and TypeScript
  configs (tsconfig.json, tsconfig.app.json, tsconfig.node.json).

Verification passed:
- npm install && npm run build && npm run test — all exit 0
- npx tsc --noEmit — zero type errors
- npx eslint src/ — zero lint errors
- npx prettier --check . — all files clean
- npm audit --audit-level=high — zero vulnerabilities

Next cycle: Cycle 2 — Create the Docker-first build scaffold
  (build/Dockerfile, build/build.sh)

---

## Cycle 2 — Create the Docker-first build scaffold
Date: 2026-03-18

Summary:
- Added build/Dockerfile: multi-stage build using node:20-alpine for the builder
  stage (npm ci + npm run build) and nginx:1.27-alpine as the runner stage
  serving dist on port 80.
- Added build/build.sh: POSIX bash script with set -euo pipefail; derives
  PROJECT_ROOT from script location; runs docker build with the repo root as
  context and tags the image mnemonic-admin:latest.
- Set build/build.sh as executable (chmod +x).

Verification passed:
- test -f build/Dockerfile && test -f build/build.sh — both files present

Next cycle: Cycle 3 — Add the CI workflow definition
  (.github/workflows/frontend-ci.yaml)

---

## Cycle 3 — Add the CI workflow definition
Date: 2026-03-18

Summary:
- Created .github/workflows/frontend-ci.yaml.
- Workflow triggers on push and pull_request (all branches).
- Single build job checks out the repo and runs ./build/build.sh.
- No inline build logic — all Docker build steps remain in the script.

Verification passed:
- test -f .github/workflows/frontend-ci.yaml — file present

Next cycle: Cycle 4 — Add routing, query client setup, and global app providers
  (src/main.tsx, src/app/App.tsx, src/app/router.tsx, src/app/providers.tsx, src/app/App.test.tsx)

---

## Cycle 4 — Add routing, query client setup, and global app providers
Date: 2026-03-18

Summary:
- Extracted provider setup into src/app/providers.tsx: AppProviders wraps
  children with QueryClientProvider (staleTime: 60000, retry: 1).
- Extracted routing into src/app/router.tsx: AppRouter renders BrowserRouter +
  Routes with one workspace route at / (WorkspacePlaceholder).
- Refactored src/app/App.tsx to compose AppProviders around AppRouter; removed
  all inline provider and route logic.
- Added src/app/App.test.tsx: component test that mounts <App /> and asserts the
  heading renders without runtime errors.

Verification passed:
- npm run build — exit 0
- npm run test — 1 test, exit 0
- npx tsc --noEmit — zero type errors
- npx eslint src/ — zero lint errors
- npx prettier --check . — all files clean

Next cycle: Cycle 5 — Implement the workspace shell and top-level split layout
  (src/app/App.tsx, src/features/workspace/PatternWorkspace.tsx,
   src/features/workspace/WorkspaceHeader.tsx,
   src/features/workspace/WorkspaceLayout.tsx,
   src/features/workspace/WorkspaceHeader.test.tsx)

---

## Cycle 5 — Implement the workspace shell and top-level split layout
Date: 2026-03-18

Summary:
- Created src/features/workspace/WorkspaceLayout.tsx: flex split-pane layout
  accepting header, leftPane, and rightPane props; left pane is w-96 overflow-auto,
  right pane is flex-1 overflow-auto; outer container is flex h-screen flex-col.
- Created src/features/workspace/WorkspaceHeader.tsx: header with "Mnemonic Admin"
  heading and "Import Pattern" button; exposes onImportClick prop.
- Created src/features/workspace/PatternWorkspace.tsx: composes WorkspaceLayout and
  WorkspaceHeader; left/right panes are empty for now.
- Updated src/app/router.tsx: replaced WorkspacePlaceholder with PatternWorkspace.
- Created src/features/workspace/WorkspaceHeader.test.tsx: tests that the heading
  renders and the Import Pattern button is present.

Verification passed:
- npm run build — exit 0
- npm run test — 3 tests (App.test.tsx + WorkspaceHeader.test.tsx), exit 0
- npx tsc --noEmit — zero type errors
- npx eslint src/ — zero lint errors
- npx prettier --check . — all files clean
- npm audit --audit-level=high — zero vulnerabilities

Next cycle: Cycle 6 — Create typed Mnemonic API models and client functions
  (src/features/patterns/api/types.ts, src/features/patterns/api/client.ts,
   src/features/patterns/api/client.test.ts)

---

## Cycle 6 — Create typed Mnemonic API models and client functions
Date: 2026-03-18

Summary:
- Created src/features/patterns/api/types.ts: all domain types including
  PatternListItem, PatternDetail, AgentAssociation, RelatedPattern,
  ChunkSummary, PaginatedResponse<T>, SearchResponse, BrowseParams,
  SearchParams, CreatePatternBody, ProblemDetail, and ApiError.
- Created src/features/patterns/api/client.ts: five exported client functions
  (listPatterns, searchPatterns, getPattern, getPatternChunks, createPattern)
  plus toApiError helper. BASE_URL reads VITE_API_BASE_URL env with fallback.
  Private apiFetch<T> handles happy path and error normalization. Problem-detail
  errors normalized to ApiError shape.
- Created src/features/patterns/api/client.test.ts: 20 tests covering toApiError
  (5 cases), each client function success and error paths, query param
  construction, POST body, and URL encoding. Uses vi.spyOn(globalThis, 'fetch').

Verification passed:
- npm run build — exit 0 (23 tests total, 260 kB bundle)
- npm run test — 23/23 passed
- npx tsc --noEmit — zero type errors
- npx eslint src/ — zero lint errors
- npx prettier --check . — all files clean
- npm audit --audit-level=high — zero vulnerabilities

Next cycle: Cycle 7 — Load and render the default browse list in the discovery rail
  (src/features/workspace/PatternWorkspace.tsx,
   src/features/patterns/components/PatternResultsList.tsx,
   src/features/patterns/components/PatternResultRow.tsx,
   src/features/patterns/components/PatternResultsList.test.tsx)

---

## Cycle 7 — Load and render the default browse list in the discovery rail
Date: 2026-03-18

Summary:
- Created src/features/patterns/components/PatternResultRow.tsx: renders a single
  PatternListItem as a <li> with name, description, tags as <span> elements, and
  a compact secondary metadata line (language · domain · entity_type · version).
- Created src/features/patterns/components/PatternResultsList.tsx: uses useQuery
  with key ['patterns'] to call listPatterns(); renders "Loading…", error, empty
  state, and a <ul> of PatternResultRow elements.
- Created src/features/patterns/components/PatternResultsList.test.tsx: 4 tests
  covering rows from mock data, empty state, loading state, and error state.
  Each test uses its own QueryClient with retry: false; listPatterns mocked via
  vi.mock.
- Updated src/features/workspace/PatternWorkspace.tsx: leftPane now renders
  <PatternResultsList /> instead of null.

Verification passed:
- npm run build — exit 0 (27 tests, 272 kB bundle)
- npm run test — 27/27 passed
- npx tsc --noEmit — zero type errors
- npx eslint src/ — zero lint errors
- npx prettier --check . — all files clean
- npm audit --audit-level=high — zero vulnerabilities

Next cycle: Cycle 8 — Add the semantic search form and search submission flow
  (src/features/patterns/components/PatternSearchForm.tsx,
   src/features/workspace/PatternWorkspace.tsx,
   src/features/patterns/components/PatternSearchForm.test.tsx)

---

## Cycle 8 — Add the semantic search form and search submission flow
Date: 2026-03-18

Summary:
- Created src/features/patterns/components/PatternSearchForm.tsx: controlled form
  with labelled text input and submit button; accepts onSearch and activeQuery props;
  shows active query when non-empty; trims whitespace before calling onSearch.
- Created src/features/patterns/components/PatternSearchForm.test.tsx: 6 tests
  covering input rendering, submit via button click, submit via Enter key, active
  query visibility, active query region absence when empty, and whitespace trimming.
- Updated src/features/patterns/components/PatternResultsList.tsx: added optional
  query prop; when non-empty calls searchPatterns({ q: query }) with key
  ["patterns", "search", query] and maps SearchResponse.results; falls back to
  listPatterns() behavior unchanged.
- Updated src/features/workspace/PatternWorkspace.tsx: added activeQuery state;
  composes PatternSearchForm above PatternResultsList in leftPane; wires onSearch
  to setActiveQuery and passes activeQuery as query to PatternResultsList.

Verification passed:
- npm run build — exit 0 (33 tests, 273 kB bundle)
- npm run test — 33/33 passed (5 test files)
- npx tsc --noEmit — zero type errors
- npx eslint src/ — zero lint errors
- npx prettier --check . — all files clean
- npm audit --audit-level=high — zero vulnerabilities

Next cycle: Cycle 9 — Add lightweight search filters for phase-1 fields
  (src/features/patterns/components/PatternFilters.tsx,
   src/features/workspace/PatternWorkspace.tsx,
   src/features/patterns/components/PatternFilters.test.tsx)
